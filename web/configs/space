server {
    listen 80;
    server_name 192.168.43.53;
 
    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/space.sock;
    }
    location /static {
        deny index.html admin.html;
        root /home/pi/Rpi-and-arduino/web;
    }
}
